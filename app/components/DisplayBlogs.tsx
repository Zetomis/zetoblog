"use client";

import useBlogs from "../hooks/useBlogs";
import { UserInterface } from "../libs/types";

const DisplayBlogs = (user: UserInterface) => {
    return <div>Blogs</div>;
};

export default DisplayBlogs;
